#include <stdio.h>
/**
 * main - Entry point
 * Description:'prints the lowercase alphabet in reverse'
 * Return: always 0
 */
int main(void)
{
	int n;

	for (n == 9; n >= 0; n--)
	{
		putchar(n + 48);
	}
	putchar('\n');
	return (0);

}
